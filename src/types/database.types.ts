// Placeholder — regenerate after applying migrations against a real Supabase
// project with:
//   npx supabase gen types typescript --project-id <project-ref> > src/types/database.types.ts
export type Database = Record<string, unknown>;
